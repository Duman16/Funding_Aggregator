apiVersion: 1

providers:
  - name: "Funding Aggregator"
    orgId: 1
    folder: "Funding Aggregator"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/dashboards
